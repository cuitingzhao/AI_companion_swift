- 在提交计划时的 loading 会比较久“正在根据目标创建计划...." ，需要有一个动画
- 删除账号
- 如果用户完成了最后的一个 milestone，我们应该显示的完成了目标的庆祝动画
- 用户达成里程碑之后的庆祝动画
- 用户里程碑due date 当天，应该有一个交互，询问用户是否已经达成里程碑，还是需要延后。
- 用户完成任务之后的庆祝动画
- 全部任务完成或者没有任务时，应该显示一个图
- 点击推迟时，没有让选择推迟的时间
- "你当前还没有可用的目标计划。" & “目标计划尚未生成”页面完全没有设计
- “今日待执行的小行动”页面可以省去，如果不省去的话，需要调整一下 UI，现在卡片即便在没有第二个目标的情况下也在最左边
- 设置：Notification（这个要在 onboarding 的时候获取权限），用户资料，删除账号
- 图片功能

todo完成：
- 性格确认这部分的跳过按钮没有 action，需要直接跳到下一个单元
- 性格确认这部分，如果没进行反馈的话，不能点击确认
- 性格确认这部分好像没有最终页（承上启下）
- KYC部分跳过按钮没有关联 action，需要关联 SKIP API
- KYC部分软键盘弹出之后，虽然整体上移了，但还是看不到输入栏
- Personality summary： 在feedback 之后，后端应该加一个接口根据用户的反馈来 summarize 用户的性格，也存到用户的性格表里
- Dialog 组件文字应该从左开始，而不是居中
- Dialog 组件应该有一个标题 
- KYCEndView的文字也应该有 typewriter 的效果
- 性格 review 页面，如果确认无法点击的话，应该换个颜色，可以参考nicknameview
- KYCchat 页面，应该显示一个 loading 状态显示 AI 正在输入
- 按住说话的按钮，按住时应该显示一个深一些的颜色
- 现在没有一个很好的机制来通知后端已经进入到了goal split 环节，跟 AI 讨论一下解决方案，如果没有好的方案则在 loading 时间过久时给一个提示
- “为你生成生辰八字中”字体可以优化一下，上下浮动
- 性格结果和 KYC 结果的页面，message 已经在白色框里了，外边的元素（按钮）是否考虑不要颜色
- KYC 聊天页面也应该加上目标 setting 页面的 loading 标志，可以考虑这个 loading 是否可以作为一个组件
- Milestone 和 task 的字体小一点
- 在性格确认的最后一页，点击确认后，会卡住，过一会才会进入下一个页面
- Milestone 下面的竖线长度貌似是固定的，在视觉上没有把所有task 包含进去
- 流式
- 根据不同的signal，显示不同的回复字样

Done
- 修改之后需要有一个 toast 提示 （从底部）
- 修改之后没有更新页面，看到的还是老的
- 输入框用白色
- 截止日期用中文
- 优先级靠左边
- goal/milstone状态没看到更改选项，task 不用。 
- 用户需要手动完成 milestone 。如果我们检测到用户的 milestone 已经过了 due date，会通过 dialog询问用户是否完成
|如果用户确认完成，那么我们更新 milestone 的状态为完成。如果有多个 milestone 都过期了，我们要一个一个顺序弹出。
|如果用户选择延后，那么我们更新 milestone 的状态为延后，需要用户提供一个due date 之后的日期，
- switch 大小扭曲
- “目标”前面加一个 icon